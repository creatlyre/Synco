---
phase: 38-gated-features-entitlements-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/test_gating.spec.ts
  - tests/e2e/test_budget.spec.ts
  - tests/e2e/test_shopping.spec.ts
  - tests/e2e/test_sync.spec.ts
autonomous: true
requirements:
  - GATE-E2E-01
  - GATE-E2E-02
  - GATE-E2E-03
  - GATE-E2E-04
  - BUD-E2E-01
  - BUD-E2E-02
  - BUD-E2E-03
  - SHOP-E2E-01
  - SHOP-E2E-02
  - SYNC-E2E-01
  - SYNC-E2E-02

must_haves:
  truths:
    - "Free user sees upgrade page (403) when navigating to /shopping, /budget/stats, /budget/import"
    - "Upgrade page displays feature-specific content and a link to /pricing"
    - "API request to gated endpoint returns 403 JSON with detail and X-Upgrade-URL header"
    - "Pro user loads /shopping, /budget/stats, /budget/import with 200 status"
    - "Budget overview, expenses, income pages render for authenticated users"
    - "Sync status API returns google_connected: false for test accounts"
  artifacts:
    - path: "tests/e2e/test_gating.spec.ts"
      provides: "Entitlement gating tests (free user blocked + API 403)"
      min_lines: 40
    - path: "tests/e2e/test_budget.spec.ts"
      provides: "Budget page render tests (overview, expenses, income, stats)"
      min_lines: 30
    - path: "tests/e2e/test_shopping.spec.ts"
      provides: "Shopping page render test for pro user"
      min_lines: 15
    - path: "tests/e2e/test_sync.spec.ts"
      provides: "Sync status API and calendar UI state test"
      min_lines: 15
  key_links:
    - from: "tests/e2e/test_gating.spec.ts"
      to: "upgrade_required.html"
      via: "page.goto() returns 403 + page renders upgrade template"
      pattern: "status.*403"
    - from: "tests/e2e/test_budget.spec.ts"
      to: "/budget/* routes"
      via: "page.goto() returns 200 + page content visible"
      pattern: "status.*200"
    - from: "tests/e2e/test_gating.spec.ts"
      to: "/shopping/items API"
      via: "page.request.get() returns 403 JSON"
      pattern: "Upgrade required"
---

<objective>
Write Playwright E2E test specs verifying plan-gated feature access control and page rendering for budget, shopping, and sync features.

Purpose: Ensure entitlement gating works end-to-end — free users are blocked with the correct upgrade page, pro users access features normally, and all authenticated users can reach ungated budget pages.

Output: 4 test spec files in `tests/e2e/` covering all 11 phase requirements.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-gated-features-entitlements-e2e/38-CONTEXT.md
@.planning/phases/38-gated-features-entitlements-e2e/38-RESEARCH.md

<interfaces>
<!-- Key gating mechanism from app/billing/dependencies.py -->

```python
class UpgradeRedirect(Exception):
    def __init__(self, feature: str = "generic"):
        self.feature = feature

def require_plan(*allowed_plans: str):
    # HTML request → raises UpgradeRedirect(feature) → renders upgrade_required.html (403)
    # API request → HTTPException(403, "Upgrade required", headers={"X-Upgrade-URL": "/billing/settings"})

_PATH_FEATURE_MAP = {
    "/shopping": "shopping",
    "/budget/stats": "budget_stats",
    "/budget/import": "budget_import",
}
```

<!-- From main.py: exception handler -->
```python
async def _upgrade_redirect_handler(request: Request, exc: UpgradeRedirect):
    context = inject_template_i18n(request, {"request": request, "feature": exc.feature})
    return templates.TemplateResponse(
        request=request, name="upgrade_required.html", context=context, status_code=403,
    )
```

<!-- From upgrade_required.html: key verifiable elements -->
- Feature headline: `<h2>` with text from `upgrade.{feature}_headline`
- CTA link: `<a href="/pricing">` (See plans button)
- Feature-specific icons differ by feature variable (shopping, budget_stats, budget_import)
- Lock label with SVG lock icon

<!-- Gated routes -->
- `/shopping` → require_plan("pro", "family_plus") in app/shopping/views.py
- `/budget/stats` → require_plan("pro", "family_plus") in app/budget/overview_views.py
- `/budget/import` → require_plan("pro", "family_plus") in app/budget/overview_views.py

<!-- Ungated budget routes -->
- `/budget/overview` → all authenticated users
- `/budget/expenses` → all authenticated users
- `/budget/income` → all authenticated users
- `/budget/settings` → all authenticated users

<!-- Sync status API response shape -->
```json
{
  "status": "google_not_connected" | "oauth_not_configured" | "ready",
  "google_connected": false,
  "oauth_configured": true,
  "household_size": 1,
  "calendar_last_sync": null
}
```

<!-- Playwright projects from Phase 36 -->
- `free` — free tier user, authenticated storage state
- `pro` — pro tier user, authenticated storage state
- `family-plus` — family plus tier user, authenticated storage state
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entitlement gating tests — free user blocked + API 403</name>
  <files>tests/e2e/test_gating.spec.ts</files>
  <read_first>
    - app/billing/dependencies.py (gating mechanism, _PATH_FEATURE_MAP, UpgradeRedirect)
    - app/templates/upgrade_required.html (page structure, CTA link href, feature variable usage)
    - main.py lines 153-166 (_upgrade_redirect_handler, 403 status code)
    - .planning/phases/38-gated-features-entitlements-e2e/38-CONTEXT.md (locked decisions)
    - .planning/phases/38-gated-features-entitlements-e2e/38-RESEARCH.md (Playwright patterns)
    - .planning/phases/36-e2e-test-infrastructure/36-CONTEXT.md (project names, auth strategy)
  </read_first>
  <action>
Create `tests/e2e/test_gating.spec.ts` with the following test structure:

**Import:** `import { test, expect } from '@playwright/test';`

**Test suite 1: "Entitlement Gating — Free User Blocked"**
Use the `free` Playwright project. For each of the 3 gated routes, write a test:

1. **"free user gets 403 on /shopping"**
   - `const response = await page.goto('/shopping');`
   - `expect(response?.status()).toBe(403);`
   - Verify upgrade page content: `await expect(page.locator('a[href="/pricing"]')).toBeVisible();`
   - Verify feature-specific content is present (the page renders, not a blank 403)

2. **"free user gets 403 on /budget/stats"**
   - Same pattern as above with `/budget/stats`

3. **"free user gets 403 on /budget/import"**
   - Same pattern as above with `/budget/import`

**Test suite 2: "Entitlement Gating — API 403 Response"**
Use the `free` Playwright project:

4. **"API returns 403 JSON with upgrade header"**
   - `const response = await page.request.get('/shopping/items');`
   - `expect(response.status()).toBe(403);`
   - `const body = await response.json();`
   - `expect(body.detail).toBe('Upgrade required');`
   - `const headers = response.headers();`
   - `expect(headers['x-upgrade-url']).toBe('/billing/settings');`

**Test suite 3: "Entitlement Gating — Pro User Allowed"**
Use the `pro` Playwright project:

5. **"pro user loads /shopping with 200"**
   - `const response = await page.goto('/shopping');`
   - `expect(response?.status()).toBe(200);`

6. **"pro user loads /budget/stats with 200"**
   - Same pattern with `/budget/stats`

7. **"pro user loads /budget/import with 200"**
   - Same pattern with `/budget/import`

**IMPORTANT:** Each `test()` call should use descriptive names. Use `test.describe()` to group by concern. The `free` and `pro` project selection is handled by `playwright.config.ts` — tests run under the project matching their storage state. Use `test.use({ storageState: ... })` or rely on the project config from Phase 36.

Check Phase 36 context for how projects are configured. If projects map to storage state files, each test.describe block should specify which project it runs under, OR separate desc blocks should be tagged appropriately.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/test_gating.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/test_gating.spec.ts exists and contains at least 7 test() calls
    - File contains `expect(response?.status()).toBe(403)` for /shopping, /budget/stats, /budget/import
    - File contains `expect(response?.status()).toBe(200)` for pro user on same routes
    - File contains `page.request.get` call for API-level 403 test
    - File contains assertion for `'Upgrade required'` in JSON body
    - File contains assertion for `x-upgrade-url` header value `/billing/settings`
    - File contains `a[href="/pricing"]` selector check for upgrade page CTA
    - All tests pass when run with `npx playwright test tests/e2e/test_gating.spec.ts`
  </acceptance_criteria>
  <done>
    Free user gets 403 + upgrade page on all 3 gated routes. API returns 403 JSON with correct body and header. Pro user loads all 3 gated routes with 200. All tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Budget, shopping, and sync render tests</name>
  <files>tests/e2e/test_budget.spec.ts, tests/e2e/test_shopping.spec.ts, tests/e2e/test_sync.spec.ts</files>
  <read_first>
    - app/budget/overview_views.py (overview, stats, import routes and template names)
    - app/budget/expense_views.py (expenses route and template name)
    - app/budget/income_views.py (income route and template name)
    - app/budget/views.py (settings route and template name)
    - app/shopping/views.py (shopping route and template name)
    - app/sync/routes.py (GET /api/sync/status response shape, google_connected field)
    - app/templates/calendar.html (sync status section, #sync-status element)
    - .planning/phases/38-gated-features-entitlements-e2e/38-CONTEXT.md (locked decisions)
    - .planning/phases/38-gated-features-entitlements-e2e/38-RESEARCH.md (Playwright patterns, sync status JSON shape)
    - .planning/phases/36-e2e-test-infrastructure/36-CONTEXT.md (project names, auth strategy)
  </read_first>
  <action>
Create 3 test files. All use the `pro` Playwright project (has full feature access and data).

**File 1: `tests/e2e/test_budget.spec.ts`**

```
import { test, expect } from '@playwright/test';
```

Test suite: "Budget Pages — Render Verification"

1. **"budget overview loads with 200"**
   - `const response = await page.goto('/budget/overview');`
   - `expect(response?.status()).toBe(200);`
   - Verify a heading or content container is visible (use `page.locator('h1, h2, .page-heading')` or similar — read the actual template to find exact selectors)

2. **"budget expenses page loads with 200"**
   - Navigate to `/budget/expenses`, assert 200, verify content area visible

3. **"budget income page loads with 200"**
   - Navigate to `/budget/income`, assert 200, verify content area visible

4. **"budget stats page loads for pro user with 200"**
   - Navigate to `/budget/stats`, assert 200 (this is gated but pro has access)
   - Verify stats content area renders

5. **"budget settings page loads with 200"**
   - Navigate to `/budget/settings`, assert 200, verify settings form visible

**File 2: `tests/e2e/test_shopping.spec.ts`**

Test suite: "Shopping Page — Pro User Render"

1. **"shopping page loads with list container"**
   - `const response = await page.goto('/shopping');`
   - `expect(response?.status()).toBe(200);`
   - Verify shopping list container is visible (read `app/templates/shopping.html` for actual selectors)
   - Verify the page is the real shopping page, not the upgrade page (no `a[href="/pricing"]` CTA)

**File 3: `tests/e2e/test_sync.spec.ts`**

Test suite: "Sync Status — Not Connected State"

1. **"sync status API returns not connected for test account"**
   - `const response = await page.request.get('/api/sync/status');`
   - `expect(response.status()).toBe(200);`
   - `const data = await response.json();`
   - `expect(data.google_connected).toBe(false);`

2. **"calendar page shows sync status section"**
   - `const response = await page.goto('/calendar');`
   - `expect(response?.status()).toBe(200);`
   - `await expect(page.locator('#sync-status')).toBeVisible();`
   - Verify the text contains a "not connected" indication (the sync status element should show google_not_connected state text)

**IMPORTANT for all files:** Read the actual HTML templates before choosing selectors. Use the most stable selectors available (IDs > data attributes > semantic elements > CSS classes). Verify page content is not the upgrade page by checking for absence of upgrade-related elements where relevant.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/test_budget.spec.ts tests/e2e/test_shopping.spec.ts tests/e2e/test_sync.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/test_budget.spec.ts exists and contains 5 test() calls for overview, expenses, income, stats, settings
    - tests/e2e/test_shopping.spec.ts exists and contains at least 1 test() call
    - tests/e2e/test_sync.spec.ts exists and contains at least 2 test() calls
    - test_budget.spec.ts contains `expect(response?.status()).toBe(200)` for all 5 budget routes
    - test_shopping.spec.ts contains `expect(response?.status()).toBe(200)` for /shopping
    - test_sync.spec.ts contains `page.request.get('/api/sync/status')` and assertion for `google_connected` being `false`
    - test_sync.spec.ts contains `page.locator('#sync-status')` visibility check on calendar page
    - All tests pass when run with `npx playwright test tests/e2e/test_budget.spec.ts tests/e2e/test_shopping.spec.ts tests/e2e/test_sync.spec.ts`
  </acceptance_criteria>
  <done>
    Budget overview/expenses/income/stats/settings all load with 200 for authenticated pro user. Shopping page renders with content. Sync API returns google_connected: false. Calendar page shows sync status section. All tests green.
  </done>
</task>

</tasks>

<verification>
Run the full Phase 38 E2E test suite:

```bash
npx playwright test tests/e2e/test_gating.spec.ts tests/e2e/test_budget.spec.ts tests/e2e/test_shopping.spec.ts tests/e2e/test_sync.spec.ts --reporter=list
```

All tests must pass. Verify:
- Free user blocked from 3 gated routes with 403
- API returns 403 JSON with correct body/header
- Pro user accesses all gated routes with 200
- Ungated budget pages render for authenticated users
- Sync status shows not connected for test accounts
</verification>

<success_criteria>
1. Free user gets 403 + upgrade template when navigating to /shopping, /budget/stats, /budget/import
2. Upgrade page contains /pricing link and feature-specific content
3. API 403 response includes JSON body with "Upgrade required" and X-Upgrade-URL header
4. Pro user loads /shopping, /budget/stats, /budget/import with 200
5. Budget overview, expenses, income, settings pages render with 200 for authenticated users
6. Sync status API confirms google_connected: false for test accounts
7. Calendar page renders sync status UI section
8. All Playwright tests pass in CI-compatible headless mode
</success_criteria>

<output>
After completion, create `.planning/phases/38-gated-features-entitlements-e2e/38-01-SUMMARY.md`
</output>
