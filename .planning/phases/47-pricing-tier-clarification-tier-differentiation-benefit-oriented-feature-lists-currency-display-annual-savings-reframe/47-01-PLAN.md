---
phase: 47-pricing-tier-clarification
plan: 01
type: execute
wave: 1
depends_on: ["46-01"]
files_modified:
  - app/locales/en.json
  - app/locales/pl.json
  - app/templates/landing.html
  - app/templates/pricing.html
autonomous: true
requirements: [PRICE-01, PRICE-02, PRICE-03, PRICE-04]

must_haves:
  truths:
    - "Pricing feature lists describe outcomes/benefits, not just feature names"
    - "Currency symbol comes from i18n key, not hardcoded zł"
    - "Landing page pricing preview shows annual pricing option with savings badge"
    - "Each tier has clear value positioning that differentiates it from neighbors"
  artifacts:
    - path: "app/locales/en.json"
      provides: "Benefit-oriented pricing features, currency key"
    - path: "app/locales/pl.json"
      provides: "Polish benefit-oriented pricing features, currency key"
    - path: "app/templates/landing.html"
      provides: "Annual pricing toggle on landing preview"
    - path: "app/templates/pricing.html"
      provides: "Currency from i18n"
  key_links:
    - from: "app/templates/landing.html"
      to: "app/locales/*.json"
      via: "t() calls for pricing.currency"
      pattern: "t\\('pricing\\.currency"
    - from: "app/templates/pricing.html"
      to: "app/locales/*.json"
      via: "t() calls for pricing.currency"
      pattern: "t\\('pricing\\.currency"
---

<objective>
Rewrite pricing feature lists as benefit-oriented copy, replace hardcoded "zł" with i18n currency key, and add annual pricing display to landing page preview.

Purpose: Better tier differentiation and conversion through benefit-first messaging, proper i18n for currency, and surfacing the annual savings option earlier in the funnel.
Output: Updated locale files, landing.html, and pricing.html.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-pricing-tier-clarification-tier-differentiation-benefit-oriented-feature-lists-currency-display-annual-savings-reframe/47-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite pricing feature lists and add currency i18n key</name>
  <files>app/locales/en.json, app/locales/pl.json, app/templates/landing.html, app/templates/pricing.html</files>
  <action>
1. Add currency i18n key to both locale files:
   - `pricing.currency`: EN "PLN", PL "zł"
   (Use "zł" for Polish since it's the familiar symbol, "PLN" for English since international users expect ISO code)

2. Rewrite pricing feature keys in en.json with benefit framing (outcomes, not feature names):
   - `pricing.free_f1`: "Shared family calendar — see everyone's schedule" (was "Shared calendar")
   - `pricing.free_f2`: "Track monthly income & expenses" (was "Basic budget tracking")
   - `pricing.free_f3`: "1 custom category per type" (keep — already specific)
   - `pricing.free_f4`: "Shared shopping list with live sync" (was "Basic shopping list")
   - `pricing.pro_f1`: "Unlimited custom categories" (was "Unlimited categories")
   - `pricing.pro_f2`: "Visual spending charts & trends" (was "Expense charts & analytics")
   - `pricing.pro_f3`: "Add events in natural language" (was "NLP event parsing")
   - `pricing.pro_f4`: "Scan receipts with OCR" (was "OCR receipt scanning")
   - `pricing.pro_f5`: "Email reminders & notifications" (was "Email notifications")
   - `pricing.family_f1`: "Everything in Pro, plus:" (was "Everything in Pro")
   - `pricing.family_f2`: "Extended data storage" (was "Extended storage")
   - `pricing.family_f3`: "All current & future premium features" (was "All premium features")
   - `pricing.family_f4`: "Priority email support" (keep — already clear)

3. Write equivalent Polish translations in pl.json:
   - `pricing.free_f1`: "Wspólny kalendarz rodzinny — wszystko w jednym miejscu"
   - `pricing.free_f2`: "Śledzenie przychodów i wydatków"
   - `pricing.free_f3`: "1 własna kategoria na typ"
   - `pricing.free_f4`: "Wspólna lista zakupów z synchronizacją"
   - `pricing.pro_f1`: "Nielimitowane kategorie"
   - `pricing.pro_f2`: "Wykresy wydatków i trendy"
   - `pricing.pro_f3`: "Dodawanie wydarzeń językiem naturalnym"
   - `pricing.pro_f4`: "Skanowanie paragonów OCR"
   - `pricing.pro_f5`: "Powiadomienia e-mail"
   - `pricing.family_f1`: "Wszystko z Pro, a dodatkowo:"
   - `pricing.family_f2`: "Rozszerzona przestrzeń na dane"
   - `pricing.family_f3`: "Wszystkie obecne i przyszłe funkcje premium"
   - `pricing.family_f4`: "Priorytetowe wsparcie e-mail"

4. In landing.html, replace all hardcoded `zł` in pricing section with `{{ t('pricing.currency') }}`:
   - Line ~402 (Free): `0 <span ...>zł</span>` → `0 <span ...>{{ t('pricing.currency') }}</span>`
   - Line ~415 (Pro): `zł{{ t('pricing.per_month') }}` → `{{ t('pricing.currency') }}{{ t('pricing.per_month') }}`
   - Line ~432 (Family): same pattern

5. In pricing.html, replace all hardcoded `zł` with `{{ t('pricing.currency') }}`:
   - Line ~40 (Free): `zł` → `{{ t('pricing.currency') }}`
   - Line ~63 (Pro): `zł` → `{{ t('pricing.currency') }}`
   - Line ~104 (Family): `zł` → `{{ t('pricing.currency') }}`
   - Line ~154 (Self-hosted): `zł` → `{{ t('pricing.currency') }}`
  </action>
  <verify>
    <automated>python -c "import json; en=json.load(open('app/locales/en.json')); assert 'pricing.currency' in en; assert 'family' in en.get('pricing.free_f1','').lower() or 'schedule' in en.get('pricing.free_f1','').lower(), f'not benefit-oriented: {en[\"pricing.free_f1\"]}'; t=open('app/templates/landing.html').read()+open('app/templates/pricing.html').read(); hardcoded = t.count('>zł<') + t.count('>zł{'); assert hardcoded == 0, f'{hardcoded} hardcoded zł remain'; print('OK')"</automated>
  </verify>
  <done>Feature lists use benefit-oriented copy. All currency displays use i18n key.</done>
</task>

<task type="auto">
  <name>Task 2: Add annual pricing toggle to landing page preview</name>
  <files>app/templates/landing.html, app/locales/en.json, app/locales/pl.json</files>
  <action>
1. Add i18n keys:
   - `landing.pricing_monthly`: EN "Monthly", PL "Miesięcznie"
   - `landing.pricing_annual`: EN "Annual", PL "Rocznie"
   - `landing.pricing_save`: EN "Save ~17%", PL "Oszczędź ~17%"

2. In the landing pricing section (before the 3-column grid, after the subtitle), add a simple toggle:
   ```html
   <div class="flex items-center justify-center gap-3 mb-8">
     <span id="lp-label-monthly" class="text-sm font-medium text-white">{{ t('landing.pricing_monthly') }}</span>
     <button id="lp-billing-toggle" type="button" role="switch" aria-checked="false"
             class="relative inline-flex h-6 w-11 items-center rounded-full bg-white/20 transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500"
             aria-label="{{ t('landing.pricing_annual') }}">
       <span id="lp-toggle-dot" class="inline-block h-4 w-4 transform rounded-full bg-white transition-transform translate-x-1"></span>
     </button>
     <span id="lp-label-annual" class="text-sm font-medium text-gray-400">{{ t('landing.pricing_annual') }}</span>
     <span id="lp-save-badge" class="hidden px-2 py-0.5 rounded-full bg-emerald-500/20 text-emerald-300 text-xs font-medium">{{ t('landing.pricing_save') }}</span>
   </div>
   ```

3. Add inline `<script>` at end of landing.html (before `</body>`) for the toggle:
   - On click: swap `aria-checked`, move dot (`translate-x-1` ↔ `translate-x-6`), swap label colors
   - Update price values: Pro monthly=19, annual=16 (192/12); Family monthly=29, annual=24 (288/12)
   - Show/hide save badge
   - Give Pro and Family price elements IDs: `lp-pro-price` and `lp-family-price`

4. Update the Pro and Family price display in landing pricing cards to use IDs:
   - Pro: `<span id="lp-pro-price">19</span>`
   - Family: `<span id="lp-family-price">29</span>`
  </action>
  <verify>
    <automated>python -c "t=open('app/templates/landing.html').read(); assert 'lp-billing-toggle' in t, 'no billing toggle'; assert 'lp-pro-price' in t, 'no pro price id'; assert 'lp-family-price' in t, 'no family price id'; print('OK')"</automated>
  </verify>
  <done>Landing pricing preview has monthly/annual toggle. Pro shows 19→16, Family shows 29→24 with ~17% savings badge.</done>
</task>

</tasks>

<verification>
- Pricing features read as benefits, not technical names
- No hardcoded "zł" in landing or pricing templates
- Landing pricing toggle switches between monthly and annual prices
- Savings badge appears when annual selected
- All tests pass: `pytest tests/ -x -q --tb=short`
</verification>

<success_criteria>
- Each tier's feature list answers "what's in it for me?" not "what does this do?"
- Currency is fully i18n'd for future multi-currency support
- Landing page visitors see annual savings option, increasing perceived value
</success_criteria>

<output>
After completion, create `.planning/phases/47-pricing-tier-clarification-tier-differentiation-benefit-oriented-feature-lists-currency-display-annual-savings-reframe/47-01-SUMMARY.md`
</output>
