---
phase: 16-overview-month-detail
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - tests/test_overview.py
autonomous: false
requirements: [OMD-01, OMD-02, OMD-03, OMD-04, OMD-05]

must_haves:
  truths:
    - "Overview API returns onetime_items per month with id, name, amount"
    - "Empty months return empty onetime_items array"
    - "Expense CRUD from overview updates totals correctly"
  artifacts:
    - path: "tests/test_overview.py"
      provides: "Integration tests for month detail feature"
      contains: "TestMonthDetail"
  key_links:
    - from: "tests/test_overview.py"
      to: "/api/budget/overview"
      via: "authenticated_client.get"
      pattern: "onetime_items"
---

<objective>
Add integration tests for the month detail feature and verify the full flow with human testing.

Purpose: Ensure the overview API extension and accordion CRUD work correctly.
Output: New test class in test_overview.py + human-verified UI flow.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-overview-month-detail-clickable-months-showing-one-time-expense-breakdown/16-CONTEXT.md
@.planning/phases/16-overview-month-detail-clickable-months-showing-one-time-expense-breakdown/16-01-SUMMARY.md

<interfaces>
From tests/test_overview.py (existing pattern):
```python
def _seed_settings(test_db, calendar_id, rate_1=100, ...): ...
class TestOverviewCalculation:
    def test_overview_returns_12_months(self, authenticated_client, test_db, test_user_a): ...
```

From tests/conftest.py:
```python
# Fixtures: test_db, authenticated_client, test_user_a (with calendar_id)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests for month detail API extension</name>
  <files>tests/test_overview.py</files>
  <action>
Add a new test class `TestMonthDetail` to test_overview.py with these tests:

1. `test_overview_includes_onetime_items` — Seed settings + create 2 one-time expenses for month 3 via POST /api/budget/expenses. GET /api/budget/overview?year=2026. Assert months[2] (March) has `onetime_items` list with 2 entries, each having `id`, `name`, `amount`.

2. `test_onetime_items_empty_for_no_expenses` — Seed settings only. GET overview. Assert months[0].onetime_items is an empty list.

3. `test_onetime_items_excludes_recurring` — Seed settings + create 1 recurring expense (month=0) + 1 one-time expense (month=5). GET overview. Assert months[4] (May) has 1 item. Assert no month has the recurring expense in onetime_items.

4. `test_add_expense_from_overview_updates_totals` — Seed settings. POST a one-time expense for month 1. GET overview. Assert months[0].onetime_expenses equals the expense amount AND months[0].onetime_items has 1 entry.

5. `test_delete_expense_updates_overview` — Seed settings + create expense. DELETE it. GET overview. Assert the month's onetime_items is empty and onetime_expenses is 0.

6. `test_edit_expense_updates_overview` — Seed settings + create expense with amount=100. PUT to update amount=200. GET overview. Assert the item's amount is 200 and onetime_expenses is 200.

Use the existing `_seed_settings` helper. Use `authenticated_client.post("/api/budget/expenses", json={...})` for creating expenses (same pattern as existing tests).
  </action>
  <verify>
    <automated>python -m pytest tests/test_overview.py -x -v 2>&1 | Select-Object -Last 30</automated>
  </verify>
  <done>6+ new tests in TestMonthDetail class, all passing, covering OMD-01 through OMD-05 API behavior</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of accordion UI and CRUD</name>
  <files>app/templates/budget_overview.html</files>
  <action>
    Verify the accordion month detail UI works correctly in the browser.
  </action>
  <what-built>
    - Month rows in year overview are clickable with chevron indicators
    - Clicking expands an inline detail showing one-time expenses for that month
    - Accordion behavior: only one month open at a time
    - Detail supports full CRUD: add, edit (click to edit inline), delete (✕)
    - Smooth CSS expand/collapse animation
    - Polish i18n labels in the detail
  </what-built>
  <how-to-verify>
    1. Start dev server: `uvicorn main:app --reload`
    2. Navigate to http://localhost:8000/budget/overview
    3. Click any month row → should expand with chevron rotation and smooth animation
    4. Verify expense list shown (or empty message if no expenses)
    5. Click another month → previous collapses, new expands
    6. Click same month → collapses
    7. In expanded detail, use add form: type name + amount → click + → expense appears
    8. Click an expense name → edit inline → press Enter → saves
    9. Click ✕ on an expense → deletes, totals update
    10. Verify totals in the main overview table recalculate after CRUD
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <verify>Human confirms UI works</verify>
  <done>Accordion UI verified: clickable months, CRUD operations, animation all working</done>
</task>

</tasks>

<verification>
- All test_overview.py tests pass (existing + new)
- Human verifies accordion UI works with CRUD
</verification>

<success_criteria>
6+ integration tests passing for month detail. Human confirms clickable months, accordion behavior, and CRUD operations work correctly in the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/16-overview-month-detail-clickable-months-showing-one-time-expense-breakdown/16-02-SUMMARY.md`
</output>
