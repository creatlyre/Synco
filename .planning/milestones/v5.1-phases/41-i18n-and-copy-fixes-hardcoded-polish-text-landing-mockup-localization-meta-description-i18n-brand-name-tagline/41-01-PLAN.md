---
phase: 41-i18n-copy-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/locales/en.json
  - app/locales/pl.json
  - app/templates/base.html
  - app/templates/landing.html
autonomous: true
requirements: [I18N-01, I18N-02, I18N-03, I18N-04]

must_haves:
  truths:
    - "Meta description renders from locale key, not hardcoded English"
    - "Landing hero taglines display in current locale language"
    - "Landing mockup sidebar data (items, currency) display in current locale"
    - "PWA install banner text displays in current locale"
    - "Brand name in landing taglines uses t('app.name') not hardcoded 'Dobry Plan'"
  artifacts:
    - path: "app/locales/en.json"
      provides: "New i18n keys for meta desc, taglines, mockup items, PWA banner"
    - path: "app/locales/pl.json"
      provides: "New i18n keys for meta desc, taglines, mockup items, PWA banner"
    - path: "app/templates/base.html"
      provides: "i18n meta description and PWA banner"
    - path: "app/templates/landing.html"
      provides: "i18n taglines and mockup content"
  key_links:
    - from: "app/templates/base.html"
      to: "app/locales/*.json"
      via: "t() calls for meta_desc and pwa keys"
      pattern: "t\\('(app\\.meta_desc|pwa\\.install)"
    - from: "app/templates/landing.html"
      to: "app/locales/*.json"
      via: "t() calls for tagline and mockup keys"
      pattern: "t\\('landing\\.(tagline|mock_item)"
---

<objective>
Move all hardcoded Polish text and non-i18n strings in base.html and landing.html to locale JSON files with proper PL/EN translations.

Purpose: Ensure the app renders correctly in both Polish and English without hardcoded strings leaking through.
Output: Updated locale files and templates with full i18n coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-i18n-and-copy-fixes-hardcoded-polish-text-landing-mockup-localization-meta-description-i18n-brand-name-tagline/41-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys and fix base.html hardcoded strings</name>
  <files>app/locales/en.json, app/locales/pl.json, app/templates/base.html</files>
  <action>
1. Add new locale keys to both en.json and pl.json:
   - `app.meta_desc` — EN: "Dobry Plan — Smart household calendar & budget planner", PL: "Dobry Plan — Wspólny kalendarz domowy, budżet i lista zakupów"
   - `app.tagline` — EN: "Your household, finally in sync", PL: "Twój dom, wreszcie ogarnięty"
   - `pwa.install_prompt` — EN: "Install Dobry Plan for quick access", PL: "Zainstaluj Dobry Plan, aby mieć szybki dostęp"

2. In base.html line 6, replace hardcoded meta description:
   FROM: `<meta name="description" content="Dobry Plan — Smart household calendar & budget planner">`
   TO: `<meta name="description" content="{{ t('app.meta_desc') }}">`

3. In base.html PWA install banner (~line 1152), replace hardcoded 'Install Dobry Plan for quick access' with a JS-accessible i18n string. Add a hidden span or data attribute with `{{ t('pwa.install_prompt') }}` and read it in the JS that builds the banner innerHTML.
  </action>
  <verify>
    <automated>python -c "import json; en=json.load(open('app/locales/en.json')); pl=json.load(open('app/locales/pl.json')); assert 'app.meta_desc' in en; assert 'app.meta_desc' in pl; assert 'pwa.install_prompt' in en; assert 'pwa.install_prompt' in pl; print('OK')"</automated>
  </verify>
  <done>base.html meta description and PWA banner use t() calls; new keys exist in both locale files</done>
</task>

<task type="auto">
  <name>Task 2: Localize landing page taglines and mockup content</name>
  <files>app/locales/en.json, app/locales/pl.json, app/templates/landing.html</files>
  <action>
1. Add new locale keys to both en.json and pl.json:
   - `landing.tagline_shopping` — EN: "to save time at the store", PL: "aby zaoszczędzić czas w sklepie"
   - `landing.tagline_calendar` — EN: "to share a calendar that makes life easier", PL: "aby dzielić się kalendarzem ułatwiającym życie"
   - `landing.mock_item_milk` — EN: "Milk", PL: "Mleko"
   - `landing.mock_item_bread` — EN: "Bread", PL: "Chleb"
   - `landing.mock_item_butter` — EN: "Butter", PL: "Masło"
   - `landing.mock_balance` — EN: "2,340 PLN", PL: "2 340 zł"

2. In landing.html lines 104-105, replace hardcoded Polish taglines:
   FROM: `<p><span class="brand-name">Dobry Plan</span>, aby zaoszczędzić czas w sklepie</p>`
   TO: `<p><span class="brand-name">{{ t('app.name') }}</span>, {{ t('landing.tagline_shopping') }}</p>`
   Same pattern for the calendar tagline on line 105.

3. In landing.html mockup sidebar (~lines 169-173), replace hardcoded:
   - "Mleko" → `{{ t('landing.mock_item_milk') }}`
   - "Chleb" → `{{ t('landing.mock_item_bread') }}`
   - "Masło" → `{{ t('landing.mock_item_butter') }}`
   - "2,340 zł" → `{{ t('landing.mock_balance') }}`
  </action>
  <verify>
    <automated>python -c "import json; en=json.load(open('app/locales/en.json')); pl=json.load(open('app/locales/pl.json')); keys=['landing.tagline_shopping','landing.tagline_calendar','landing.mock_item_milk','landing.mock_item_bread','landing.mock_item_butter','landing.mock_balance']; [print(f'{k}: en={en[k]}, pl={pl[k]}') for k in keys]; print('OK')"</automated>
  </verify>
  <done>Landing page taglines use t('app.name') + t() calls; mockup items and balance use locale keys; all content renders correctly in both PL and EN</done>
</task>

</tasks>

<verification>
- All hardcoded Polish strings in landing.html replaced with t() calls
- Meta description in base.html uses t() call
- PWA banner text uses i18n
- Both en.json and pl.json have all new keys
- `python -m pytest tests/ -x -q` passes (no regressions)
</verification>

<success_criteria>
- Zero hardcoded Polish user-facing text in base.html and landing.html (aside from category keyword arrays which are bilingual by design)
- All new locale keys present in both en.json and pl.json
- Existing tests pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/41-i18n-and-copy-fixes-hardcoded-polish-text-landing-mockup-localization-meta-description-i18n-brand-name-tagline/41-01-SUMMARY.md`
</output>
