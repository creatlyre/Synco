---
phase: 46-landing-page-ux
plan: 01
type: execute
wave: 1
depends_on: ["45-01"]
files_modified:
  - app/templates/landing.html
  - app/templates/login.html
  - app/locales/en.json
  - app/locales/pl.json
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05]

must_haves:
  truths:
    - "Landing nav has Features link that scrolls to #features section"
    - "Mobile users see a hamburger menu that reveals Features, Pricing, and Sign in links"
    - "Login page shows a brand tagline below the app name"
    - "Footer includes a GitHub link"
    - "Muted text colors meet WCAG AA 4.5:1 contrast against #0f0a2e background"
  artifacts:
    - path: "app/templates/landing.html"
      provides: "Features nav link, mobile hamburger menu, footer GitHub link, contrast fixes"
    - path: "app/templates/login.html"
      provides: "Brand tagline subtitle"
    - path: "app/locales/en.json"
      provides: "i18n keys for tagline, Features link, GitHub link"
    - path: "app/locales/pl.json"
      provides: "Polish translations for new keys"
  key_links:
    - from: "app/templates/landing.html"
      to: "app/locales/*.json"
      via: "t() calls for new nav and footer keys"
      pattern: "t\\('landing\\.(nav_features|github)"
    - from: "app/templates/login.html"
      to: "app/locales/*.json"
      via: "t() call for auth.tagline"
      pattern: "t\\('auth\\.tagline"
---

<objective>
Improve landing page navigation UX (Features link, mobile hamburger menu), add brand tagline to login page, add GitHub link to footer, and fix text contrast issues.

Purpose: Better mobile navigation and conversion UX, stronger brand presence on login, improved accessibility compliance.
Output: Updated landing.html, login.html, and locale files.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-landing-page-ux-improvements-features-nav-link-mobile-hamburger-nav-login-tagline-footer-social-links-contrast-audit/46-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Features nav link, mobile hamburger menu, and footer GitHub link</name>
  <files>app/templates/landing.html, app/locales/en.json, app/locales/pl.json</files>
  <action>
1. Add i18n keys to en.json and pl.json:
   - `landing.nav_features`: EN "Features", PL "Funkcje"
   - `landing.github_link`: EN "GitHub", PL "GitHub"

2. In the landing nav (line ~93), add a Features link before the Pricing link:
   ```html
   <a href="#features" class="text-sm text-gray-300 hover:text-white transition-colors hidden sm:inline">{{ t('landing.nav_features') }}</a>
   ```

3. Add a mobile hamburger toggle button visible only below `sm:` breakpoint. Place it inside the nav's flex container. When clicked, toggle a dropdown menu with Features, Pricing, and Sign in links.

   Implementation approach:
   - Add a hamburger button: `<button class="sm:hidden" onclick="document.getElementById('mobile-menu').classList.toggle('hidden')" aria-expanded="false" aria-controls="mobile-menu" aria-label="Menu">`
   - Add a `<div id="mobile-menu" class="hidden sm:hidden">` below the nav's inner container with the three links stacked vertically
   - Keep it simple — no JS framework, just classList.toggle
   - Update `aria-expanded` on click

4. In the footer (line ~459), add a GitHub link after the refund link:
   ```html
   <span class="hidden md:inline text-gray-600">·</span>
   <a href="https://github.com/AlanJuaworku/CalendarPlanner" class="hover:text-white transition-colors">GitHub</a>
   ```
   Note: Use the actual GitHub repo URL from the project. If unsure, use a placeholder comment.

5. Contrast audit for landing page:
   - `text-gray-500` on #0f0a2e: ratio ~3.3:1 (FAILS AA). Replace with `text-gray-400` (ratio ~5.5:1) wherever used for readable text
   - `text-gray-400` on #0f0a2e: ratio ~5.5:1 (PASSES AA). Leave as-is.
   - Check the "dobryplan.app" address bar mockup text (line ~150) — `text-gray-500` should become `text-gray-400`
   - Check pricing note text (lines ~403, ~416, ~433) — `text-gray-500` should become `text-gray-400`
  </action>
  <verify>
    <automated>python -c "t=open('app/templates/landing.html').read(); assert 'mobile-menu' in t, 'no mobile menu'; assert '#features' in t, 'no features link'; assert 'github' in t.lower(), 'no github link'; print('OK')"</automated>
  </verify>
  <done>Landing page has Features nav link, working mobile hamburger menu, GitHub footer link, and text-gray-500 replaced with text-gray-400 for readable text.</done>
</task>

<task type="auto">
  <name>Task 2: Add brand tagline to login page</name>
  <files>app/templates/login.html, app/locales/en.json, app/locales/pl.json</files>
  <action>
1. Add i18n key to en.json and pl.json:
   - `auth.tagline`: EN "Your household, finally in sync", PL "Twój dom, wreszcie ogarnięty"

2. In login.html, after the `.brand` div (which has the logo + app name) and before the `<h1>` login title, add:
   ```html
   <p class="tagline">{{ t('auth.tagline') }}</p>
   ```

3. Add `.tagline` CSS in the existing `<style>` block:
   ```css
   .tagline {
     text-align: center;
     font-size: 0.875rem;
     color: rgba(255,255,255,0.5);
     margin-bottom: 1.5rem;
     letter-spacing: 0.01em;
   }
   ```
   Make sure the color meets WCAG AA contrast against the login background gradient (darkest part ~#0f0c29). rgba(255,255,255,0.5) on #0f0c29 gives ~5.1:1 — passes.
  </action>
  <verify>
    <automated>python -c "t=open('app/templates/login.html').read(); assert 'tagline' in t, 'no tagline'; import json; en=json.load(open('app/locales/en.json')); assert 'auth.tagline' in en; print('OK')"</automated>
  </verify>
  <done>Login page shows brand tagline "Your household, finally in sync" below the app name, in both PL and EN.</done>
</task>

</tasks>

<verification>
- Landing nav shows Features and Pricing links on desktop
- Mobile view shows hamburger menu with Features, Pricing, Sign in
- Footer shows GitHub link
- Login page shows tagline
- No `text-gray-500` on readable body text in landing.html
- All tests pass: `pytest tests/ -x -q --tb=short`
</verification>

<success_criteria>
- Mobile users can access all nav links via hamburger menu
- Desktop users see Features + Pricing links in nav
- Login page reinforces brand with tagline
- All readable text meets WCAG AA contrast 4.5:1
</success_criteria>

<output>
After completion, create `.planning/phases/46-landing-page-ux-improvements-features-nav-link-mobile-hamburger-nav-login-tagline-footer-social-links-contrast-audit/46-01-SUMMARY.md`
</output>
