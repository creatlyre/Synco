# Quick Task 260324-s45: Fix pricing page showing authenticated nav to unauthenticated users

## Goal
Public pages (/pricing, /terms, /privacy, /refund) that extend `base.html` show the full authenticated navigation (Dashboard, Calendar, Budget, Shopping, Plan, Quick Add, Logout) even when user is not logged in. Fix `base.html` nav to be auth-aware.

## Plan 1: Add conditional nav rendering in base.html

### Task 1: Add auth-conditional nav in base.html
- **files:** `app/templates/base.html`
- **action:** Wrap authenticated nav links (dashboard, calendar, budget, shopping, plan, quick add, notifications, logout) in `{% if user is defined %}` block. Add an `{% else %}` block showing public nav (logo, Pricing link, Sign In button) — matching landing.html style.
- **verify:** Visit `/pricing` while not logged in — should see only logo + Pricing + Sign In. Visit `/dashboard` while logged in — should see full nav as before.
- **done:** Public pages show public nav; authenticated pages show full nav.
